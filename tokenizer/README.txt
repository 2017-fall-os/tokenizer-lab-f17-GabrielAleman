myTok.c: This is the main file for the tokenizer
	 wordCounter() - count the ammount of words there are in thee in the string and returns it as an int;
	 mytoc() - main method that you call to get your string tokenized
	 freeMem() - method to free the tokized string
	 lengthOfWord - counts how many lettters are in a word and then goes and allocates memmory for it and inserts it in.
	 pinrtArray() - prints where the mem location is and the word


test.c: sets a bunch of strings and then calles mytok.c to tokenize them by " "
